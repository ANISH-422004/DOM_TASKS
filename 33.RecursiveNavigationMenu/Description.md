Created a dynamic multi-level menu in React with collapsible child items. Each menu item uses useState to independently manage the visibility of its children. Implemented a toggle feature (+/-) to show or hide nested submenus. Ensured that each MenuItem instance maintains its own state, allowing independent interaction without affecting other items. This task improved my understanding of React component instantiation, state management, and efficient re-rendering.